import { ToggleResultsDirective } from './toggle-results.directive';

describe('ToggleResultsDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleResultsDirective();
    expect(directive).toBeTruthy();
  });
});
